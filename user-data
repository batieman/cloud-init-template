#cloud-config

# Local demo credential. Replace this before using the template anywhere other
# than an isolated development VM.
password: password
chpasswd:
  expire: False

packages:
  - openssh-server
